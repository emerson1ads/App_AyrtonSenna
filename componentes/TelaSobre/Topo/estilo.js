import {StyleSheet} from 'react-native';

const estilo = StyleSheet.create({
    container: {
        alignItems: "center"
    },
    titulo: {
        fontSize: 26,
        marginBottom: 10
    },
    texto: {
        textAlign: 'left',
        marginHorizontal: 10,
        fontSize: 15,
        color: '#a6a6a6',
        marginBottom: 20
    },
    img: {
        width: '97%',
        height: 200,
        borderRadius: 30,
        marginBottom: 10
    }
});

export default estilo;